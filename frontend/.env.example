# Copy to frontend/.env — your Mac's LAN IP (same Wi‑Fi as your phone)
EXPO_PUBLIC_API_BASE=http://YOUR_LOCAL_IP:3000

# Voice Bistro tab: OpenAI Realtime via backend WebSocket (OPENAI_API_KEY on backend)
# Text chat tab: Claude (ANTHROPIC_API_KEY on backend)
